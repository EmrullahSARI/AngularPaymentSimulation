import { ConvertMetersPipe } from './convert-meters.pipe';

describe('ConvertMetersPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMetersPipe();
    expect(pipe).toBeTruthy();
  });
});
